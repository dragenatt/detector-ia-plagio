"""Veraz — Analizador de Originalidad.

Backend del sistema. El subpaquete `analysis` contiene el motor de análisis
(texto puro de Python, sin dependencias pesadas) y `model` contiene el
clasificador entrenable. La capa web (FastAPI) vive en `main.py`.
"""

__version__ = "0.1.0"
